{
  "name": "tddbin-frontend",
  "version": "0.0.1",
  "description": "TDDbin.com UI components (and examples)",
  "main": "index.js",
  "scripts": {
    "build": "npm run build-app && npm run build-spec-runners",
    "build-app": "browserify examples/tddbin-standalone.js --outfile=dist/tddbin-standalone.js --transform=reactify",
    "build-spec-runners": "browserify src/test-runner/mocha/spec-runner.js --outfile=dist/mocha-spec-runner.js --transform=reactify",
    "deploy": "npm run build && ./deploy-ghpages.sh",
    "lint": "jsxcs --preset google src",
    "start": "webpack-dev-server",
    "test": "npm run lint && jasmine-node src",
    "watch": "watchify examples/tddbin-standalone.js --outfile=dist/tddbin-standalone.js --transform=reactify -v"
  },
  "browserify": {
    "extension": [
      "jsx"
    ],
    "transform": [
      [
        "reactify",
        {
          "es6": true
        }
      ]
    ]
  },
  "keywords": [
    "tddbin",
    "tdd",
    "javascript"
  ],
  "bugs": {
    "url": "https://github.com/uxebu/tddbin-frontend/issues"
  },
  "author": "uxebu",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "ace-with-plugins": "git://github.com/uxebu/ace-with-plugins.git",
    "browserify": "^8.1.3",
    "jasmine-matchers": "^0.2.3",
    "jasmine-node": "^1.14.5",
    "jsxcs": "0.0.3",
    "reactify": "^1.0.0",
    "watchify": "^2.3.0",
    "react": "^0.12.2",
    "mocha": "^1.21.5",
    "referee": "^1.1.1",
    "should": "^5.0.0"
  }
}
